$('#modal-holder').html("<%= escape_javascript(render 'transactions/new') %>");
$('#modal-holder').dialog({
	dialogClass: 'popup',
	title: $("#transaction_amount").data('sub') == true ? "New Purchase" : "New Income"
});